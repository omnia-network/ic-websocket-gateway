{
  "name": "ic_websocket",
  "version": "0.1.0",
  "description": "IC WebSocket on the IC",
  "keywords": [
    "Internet Computer",
    "Rust",
    "JavaScript",
    "Canister"
  ],
  "scripts": {
    "build": "cd src/ic_websocket_frontend && npm run build",
    "prebuild": "npm run generate",
    "start": "cd src/ic_websocket_frontend && npm run dev",
    "prestart": "npm run generate",
    "generate": "dfx generate ic_websocket_backend"
  },
  "devDependencies": {
    "@dfinity/agent": "0.15.1",
    "@dfinity/candid": "0.15.1",
    "@dfinity/principal": "0.15.1",
    "assert": "2.0.0",
    "buffer": "6.0.3",
    "events": "3.3.0",
    "process": "0.11.10",
    "stream-browserify": "3.0.0",
    "util": "0.12.4"
  },
  "engines": {
    "node": "^12 || ^14 || ^16 || ^18"
  },
  "browserslist": [
    "last 2 chrome version",
    "last 2 firefox version",
    "last 2 safari version",
    "last 2 edge version"
  ]
}
